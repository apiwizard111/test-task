[project]
name = "nexus-tasks"
version = "0.4.2"
description = "In-memory task tracker API used as the RepoLens demo corpus"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "pydantic>=2.11",
    "uvicorn>=0.34",
]
